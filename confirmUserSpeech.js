let example1 = "I want to be able to say this.";
let example2 = "I went to be able to nay say this.";

//testing


function checkSpeech(userSentenceString, userOriginalText) {
  if (userSentenceString == userOriginalText){
    console.log("Perfect speaking!");
  }

  // check the user's original text with what they said
  let userSentenceStringArr = userSentenceString.split(" ");
  let userOriginalTextArr = userOriginalText.split(" ");

  // both arrays are different lengths
  if (userSentenceStringArr)
  for (int i = 0; i < userSentenceStringArr.length; i++) {
    //check each index
    if
  }
}
